<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= set.name %> - Lego Collection</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body class="bg-gray-900 text-yellow-400">
    <%- include('partials/navbar', { page: "/lego/sets" }) %>
    
    <!-- Container for entire page -->
    <div class="container mx-auto py-10 px-5">
        <!-- Hero Section -->
        <div class="bg-yellow-500 p-8 rounded-lg mb-12 text-center">
            <h1 class="text-5xl font-extrabold mb-2 text-gray-900"><%= set.name %></h1>
            <p class="text-2xl font-medium text-gray-800">Below, you will find detailed information about the set:</p>
        </div>
        
        <!-- Main Content Section -->
        <div class="flex flex-col md:flex-row items-center space-y-10 md:space-y-0 md:space-x-16">
            <!-- Image Section -->
            <div class="flex-shrink-0">
                <img src="<%= set.img_url %>" alt="<%= set.name %>" class="w-64 h-64 object-cover rounded-lg shadow-lg border-4 border-yellow-500">
            </div>
            
            <!-- Set Details Section -->
            <div class="text-lg space-y-4 text-gray-100">
                <p><strong class="text-yellow-400">Set Name:</strong> <span class="text-yellow-300"><%= set.name %></span></p>
                <p><strong class="text-yellow-400">Year Released:</strong> <span class="text-yellow-300"><%= set.year %></span></p>
                <p><strong class="text-yellow-400">Theme:</strong> <span class="text-yellow-300"><%= set.theme.name || "Unknown" %></span></p>
                <p><strong class="text-yellow-400">Number of Parts:</strong> <span class="text-yellow-300"><%= set.num_parts %></span></p>
            </div>
        </div>

        <!-- Buttons Section -->
        <div class="mt-12 text-center space-x-4">
            <a href="#" onclick="history.back(); return false;" class="inline-block px-8 py-3 bg-yellow-500 text-gray-900 rounded-lg font-semibold hover:bg-yellow-600 shadow-md">Return</a>
            <a href="/lego/editSet/<%= set.set_num %>" class="inline-block px-8 py-3 bg-green-500 text-gray-900 rounded-lg font-semibold hover:bg-green-600 shadow-md">Edit Set</a>
        </div>
    </div>
</body>
</html>
