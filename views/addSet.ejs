<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Lego Set</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body class="bg-gray-900 text-gray-100">
    <%- include('partials/navbar', { page: '/lego/addSet' }) %>
    <div class="container mx-auto px-4 py-10">
        <h1 class="text-4xl font-bold text-yellow-500 mb-6">Add a New Lego Set</h1>
        <form action="/lego/addSet" method="POST" class="bg-gray-800 p-6 rounded-lg">
            <div class="mb-4">
                <label for="name" class="block text-gray-200 mb-2">Set Name:</label>
                <input type="text" id="name" name="name" class="w-full p-3 rounded-lg bg-gray-700 text-gray-100" required>
            </div>
            <div class="mb-4">
                <label for="year" class="block text-gray-200 mb-2">Year:</label>
                <input type="number" id="year" name="year" class="w-full p-3 rounded-lg bg-gray-700 text-gray-100" required>
            </div>
            <div class="mb-4">
                <label for="num_parts" class="block text-gray-200 mb-2">Number of Parts:</label>
                <input type="number" id="num_parts" name="num_parts" class="w-full p-3 rounded-lg bg-gray-700 text-gray-100" required>
            </div>
            <div class="mb-4">
                <label for="img_url" class="block text-gray-200 mb-2">Image URL:</label>
                <input type="url" id="img_url" name="img_url" class="w-full p-3 rounded-lg bg-gray-700 text-gray-100" required>
            </div>
            <div class="mb-4">
                <label for="theme_id" class="block text-gray-200 mb-2">Theme:</label>
                <select id="theme_id" name="theme_id" class="w-full p-3 rounded-lg bg-gray-700 text-gray-100" required>
                    <% themes.forEach(theme => { %>
                        <option value="<%= theme.id %>"><%= theme.name %></option>
                    <% }) %>
                </select>
            </div>
            <div class="mb-4">
                <label for="set_num" class="block text-gray-200 mb-2">Set Number:</label>
                <input type="text" id="set_num" name="set_num" class="w-full p-3 rounded-lg bg-gray-700 text-gray-100" required>
            </div>
            <button type="submit" class="bg-yellow-500 text-black px-4 py-2 rounded-lg hover:bg-yellow-600">Add Set</button>
        </form>
    </div>
</body>
</html>
